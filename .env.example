# Copy this file to .env — it is gitignored, so your real values stay local.
#
#   cp .env.example .env

# Matches the credentials in docker-compose.yml.
DATABASE_URL="mysql://botguide:botguide@localhost:3306/botguide_kanban"
